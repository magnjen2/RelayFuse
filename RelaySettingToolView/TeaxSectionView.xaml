<UserControl x:Class="RelaySettingToolView.TeaxSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RelaySettingToolView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ViewResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid>
            <Button x:Name="SaveTextFileButton"
                    Width="35"
                    Height="35"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="10"
                    ToolTip="Export teax"
                    Style="{StaticResource RoundedButtonStyle}">
                <TextBlock FontFamily="Segoe MDL2 Assets"
                           FontSize="24"
                           Text="&#xE74E;"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"/>
            </Button>
        </Grid>
        <Border CornerRadius="15" Background="{StaticResource WindowBackground}" Padding="20" Margin="50">
            <TextBlock Text="TEAX Section Content" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <!-- Add your TEAX content here -->
        </Border>
    </Grid>
</UserControl>
