<!--<UserControl x:Class="RelaySettingToolView.DataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RelaySettingToolView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:SelectedCellBackgroundConverter x:Key="SelectedCellBackgroundConverter"/>
    </UserControl.Resources>

    <DataGrid ItemsSource="{Binding GridRows}" AutoGenerateColumns="False">
        <DataGrid.Columns>
            --><!-- Column1 --><!--
            <DataGridTemplateColumn Header="Unique id">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Border
                            x:Name="Column1Border"
                            DataContext="{Binding Column1}"
                            Background="{Binding IsSelected, Converter={StaticResource SelectedCellBackgroundConverter}}"
                            MouseLeftButtonDown="Cell_MouseLeftButtonDown">
                            <TextBlock x:Name="Column1Text" Text="{Binding Content}" FontFamily="Segoe UI" Margin="2,0" />
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsGridOverlay}" Value="True">
                                <Setter TargetName="Column1Text" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            --><!-- Column2 --><!--
            <DataGridTemplateColumn Header="Display name">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Border
                            x:Name="Column2Border"
                            DataContext="{Binding Column2}"
                            Background="{Binding IsSelected, Converter={StaticResource SelectedCellBackgroundConverter}}"
                            MouseLeftButtonDown="Cell_MouseLeftButtonDown">
                            <TextBlock x:Name="Column2Text" Text="{Binding Content}"  FontFamily="Segoe UI" Margin="2,0" />
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsGridOverlay}" Value="True">
                                <Setter TargetName="Column2Text" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsGridOverlay}" Value="False">
                                <Setter TargetName="Column2Text" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            --><!-- Column3 value --><!--
            <DataGridTemplateColumn Header="Setting value">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Border
                            x:Name="Column3Border"
                            DataContext="{Binding Column3}"
                            Background="{Binding IsSelected, Converter={StaticResource SelectedCellBackgroundConverter}}"
                            MouseLeftButtonDown="Cell_MouseLeftButtonDown">
                            <TextBlock x:Name="Column3Text" Text="{Binding Content}" FontFamily="Segoe UI"  Margin="2,0"/>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsGridOverlay}" Value="True">
                                <Setter TargetName="Column3Text" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            --><!-- Column4 --><!--
            <DataGridTemplateColumn Header="Unit">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Border
                            x:Name="Column4Border"
                            DataContext="{Binding Column4}"
                            Background="{Binding IsSelected, Converter={StaticResource SelectedCellBackgroundConverter}}"
                            MouseLeftButtonDown="Cell_MouseLeftButtonDown">
                            <TextBlock x:Name="Column4Text" Text="{Binding Content}"  FontFamily="Segoe UI"  Margin="2,0"/>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsGridOverlay}" Value="True">
                                <Setter TargetName="Column4Text" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
        --><!-- Overlay OverlayText across all columns when IsGridOverlay is true --><!--
        <DataGrid.RowStyle>
            <Style TargetType="DataGridRow">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridRow">
                            <Grid>
                                <Border x:Name="TopLine"
                                BorderBrush="Black"
                                BorderThickness="0,1,0,0"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Bottom"/>
                                <DataGridCellsPresenter x:Name="CellsPresenter"/>
                                <TextBlock x:Name="OverlayTextOverlay"
                                           Text="{Binding OverlayText.Content}"
                                           FontFamily="Segoe UI Bold"
                                           HorizontalAlignment="Stretch"
                                           VerticalAlignment="Center"
                                           Margin="0"
                                           Padding="4"
                                           Visibility="Collapsed"
                                           TextTrimming="CharacterEllipsis"
                                           TextWrapping="Wrap"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsGridOverlay}" Value="True">
                                    <Setter TargetName="CellsPresenter" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="OverlayTextOverlay" Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </DataGrid.RowStyle>
    </DataGrid>
</UserControl>-->